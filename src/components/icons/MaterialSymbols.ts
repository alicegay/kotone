import { createIconSet } from 'react-native-vector-icons'
import glyphmap from 'assets/glyphmaps/MaterialSymbols.json'

const Icon = createIconSet(
  glyphmap,
  'Material Symbols Rounded',
  'MaterialSymbolsRounded.ttf',
)

export default Icon
