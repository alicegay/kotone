import { createIconSet } from 'react-native-vector-icons'
import glyphmap from 'assets/glyphmaps/MaterialIcons.json'

const Icon = createIconSet(
  glyphmap,
  'Material Icons Round',
  'MaterialIconsRound.otf',
)

export default Icon
